@sync
Service(platform Platform)
  setCredentials(account string, password string, syncKey string)
  register(Store) -> Engine
  unregister(Engine)
  startSync()
  reset(firstSync)

@private_ctor
Engine
  startSync()

